# このファイルを .env にコピーして値を入れてください。
#   cp .env.example .env
# .env は .gitignore 済みなので GitHub には上がりません。

# Claude API（LLMPredictor が使う）https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Kaggle（kaggle auth login を使う場合は不要）
KAGGLE_API_TOKEN=
